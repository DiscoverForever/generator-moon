/** Dont remove this file 'import from' will inject there */
<%_ statemachines.forEach(statemachine => {_%>
import './<%= _.kebabCase(statemachine.scxml.$.entityname) %>/<%= _.kebabCase(statemachine.scxml.$.enumname) %>.statemachine';
<%_ }); _%>

export default {}